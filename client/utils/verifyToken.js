// if context.user.user_id !== user_id of any Users, then run addUser otherwise, run query for savedCities

const checkUserId = (user) => {
    if user.user_id 
    //run a query to DB 
    
};

export default checkUserId;
